// This structure has no fields for now, but is still named like it has them.
// This is because eventually it might get runtime-loaded assets as fields.

pub const templates = @import("Assets/templates.zig");

const std = @import("std");
